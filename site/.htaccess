Options -Indexes
AddDefaultCharset UTF-8

# PHP session безопасность
php_value session.cookie_httponly 1
php_value session.use_strict_mode 1

# Запрет прямого доступа к config/
<FilesMatch "^(db\.php)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
